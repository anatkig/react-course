import type { QuizQuestion } from '../types';

const levelTestPool: QuizQuestion[] = [
  { id: 'lt-1', question: 'What is JSX in React?', options: ['A syntax extension that allows writing HTML-like code in JavaScript', 'A query language designed for fetching and managing database records', 'A preprocessor that converts custom syntax into standard CSS rules', 'A testing framework that automatically validates component behavior'], correctAnswer: 0, explanation: 'JSX is a syntax extension for JavaScript that lets you write HTML-like markup inside a JavaScript file.' },
  { id: 'lt-2', question: 'Which hook is used to manage state in a functional component?', options: ['useEffect', 'useRef', 'useState', 'useContext'], correctAnswer: 2, explanation: 'useState is the primary hook for adding state variables to functional components.' },
  { id: 'lt-3', question: 'What does the virtual DOM do?', options: ['Replaces the browser DOM entirely by using a custom rendering engine', 'Provides a lightweight copy of the real DOM to optimize updates', 'Stores component data on the server and retrieves it via an API', 'Compiles JSX templates directly into static HTML pages at build time'], correctAnswer: 1, explanation: 'The virtual DOM is a lightweight JavaScript representation of the real DOM used to determine the minimal set of changes needed.' },
  { id: 'lt-4', question: 'What is the purpose of the key prop when rendering lists?', options: ['It helps React identify which items have changed, been added, or removed', 'It applies CSS class names to list items based on their position index', 'It determines the visual ordering and sort direction of list elements', 'It encrypts list data before transmitting it between parent and child'], correctAnswer: 0, explanation: 'Keys help React identify which items in a list have changed. They should be stable, unique identifiers among siblings.' },
  { id: 'lt-5', question: 'Which method is used to pass data from a parent to a child component?', options: ['State', 'Context', 'Props', 'Refs'], correctAnswer: 2, explanation: 'Props are the primary mechanism for passing data from parent to child components.' },
  { id: 'lt-6', question: 'When does useEffect with an empty dependency array run?', options: ['Only on the first render (mount)', 'On every single render cycle the component goes through', 'Only when any piece of component state changes', 'Never — empty arrays disable the effect entirely'], correctAnswer: 0, explanation: 'useEffect with [] runs only once after the initial render (mount).' },
  { id: 'lt-7', question: 'What is the correct way to update state based on previous state?', options: ['setState(state + 1) direct value', 'setState(prev => prev + 1)', 'state = state + 1 assignment', 'this.state += 1 increment'], correctAnswer: 1, explanation: 'Use the functional updater form to ensure you work with the latest state value.' },
  { id: 'lt-8', question: 'What is a controlled component in React?', options: ['A form element whose value is driven by React state', 'A component that manages and orchestrates other components', 'A component that receives no props from its parent component', 'A component that accesses DOM elements directly through refs'], correctAnswer: 0, explanation: 'A controlled component is a form element whose value is controlled by React state and updated via onChange.' },
  { id: 'lt-9', question: 'What does React.memo do?', options: ['Stores component data in localStorage and retrieves it on mount', 'Memoizes a component to skip re-renders when props haven\'t changed', 'Creates a mutable ref that persists across component re-renders', 'Wraps a component with an error boundary for graceful fallbacks'], correctAnswer: 1, explanation: 'React.memo skips re-rendering when props haven\'t changed (shallow comparison).' },
  { id: 'lt-10', question: 'Which hook shares state across deeply nested components without prop drilling?', options: ['useState', 'useReducer', 'useContext', 'useRef'], correctAnswer: 2, explanation: 'useContext reads context values created with createContext, avoiding prop drilling.' },
  { id: 'lt-11', question: 'What is the purpose of useReducer?', options: ['To manage complex state logic with a reducer function', 'To reduce the total number of components in the tree', 'To reduce the final JavaScript bundle size at build time', 'To optimize rendering speed by batching DOM operations'], correctAnswer: 0, explanation: 'useReducer manages complex state with multiple sub-values or interdependent transitions.' },
  { id: 'lt-12', question: 'What is the children prop in React?', options: ['An array of child component class definitions passed as a prop', 'Content placed between opening and closing tags of a component', 'A lifecycle method that executes when children mount or unmount', 'A required prop that every component must declare and consume'], correctAnswer: 1, explanation: 'The children prop contains whatever JSX is placed between the opening and closing tags of a component.' },
  { id: 'lt-13', question: 'What happens if you mutate state directly (e.g., state.push(item))?', options: ['React detects the mutation and triggers an immediate re-render', 'The application throws a runtime error and crashes immediately', 'React does not detect the change and the UI won\'t update', 'It works in development mode but silently fails in production'], correctAnswer: 2, explanation: 'React uses reference comparison. Mutating in place keeps the same reference, so React won\'t re-render.' },
  { id: 'lt-14', question: 'Which is NOT a valid way to conditionally render in React?', options: ['{condition && <Component />}', '{condition ? <A /> : <B />}', 'if (condition) return <Component />', '<if condition={true}><Component /></if>'], correctAnswer: 3, explanation: 'There is no <if> element in JSX. Use JavaScript expressions like &&, ternary, or early returns.' },
  { id: 'lt-15', question: 'What is the difference between useEffect and useLayoutEffect?', options: ['useLayoutEffect runs synchronously after DOM mutations but before paint; useEffect runs after paint', 'They are functionally identical and can always be used interchangeably', 'useLayoutEffect is the hook version designed only for class component lifecycle methods', 'useEffect fires before the render phase while useLayoutEffect fires during reconciliation'], correctAnswer: 0, explanation: 'useLayoutEffect fires synchronously after DOM mutations but before the browser paints.' },
  { id: 'lt-16', question: 'What does useRef return?', options: ['A reactive state variable that triggers a re-render whenever its value changes', 'A memoized callback function reference that changes only when dependencies update', 'A mutable ref object with a .current property that persists across renders', 'A context value object that propagates updates down through the component tree'], correctAnswer: 2, explanation: 'useRef returns a mutable object whose .current property persists across renders without causing re-renders.' },
  { id: 'lt-17', question: 'What is prop drilling?', options: ['A performance optimization technique that reduces unnecessary re-renders', 'A pattern where too many props are added to a single component interface', 'Manipulating DOM elements directly by drilling into the element hierarchy', 'Passing props through many intermediate components that don\'t use them'], correctAnswer: 3, explanation: 'Prop drilling is passing props through multiple component levels just to reach a deeply nested child.' },
  { id: 'lt-18', question: 'How do you prevent a form from reloading the page on submit?', options: ['Use type="button" on the submit button instead of type="submit"', 'Call e.preventDefault() in the onSubmit handler', 'Return false from the event handler to stop the default action', 'Remove the action attribute entirely from the form element tag'], correctAnswer: 1, explanation: 'Calling e.preventDefault() prevents the browser\'s default form submission behavior.' },
  { id: 'lt-19', question: 'What does React.lazy() do?', options: ['Loads a component dynamically via code splitting', 'Delays the rendering of a component until the next idle frame', 'Memoizes a component and caches its rendered output in memory', 'Creates a placeholder component that renders while data is loading'], correctAnswer: 0, explanation: 'React.lazy() enables code splitting by loading a component only when it\'s first rendered.' },
  { id: 'lt-20', question: 'What is the Suspense component used for?', options: ['Catching and handling JavaScript errors thrown during the render phase', 'Providing a global state management solution across the component tree', 'Showing fallback content while lazy-loaded components are loading', 'Defining route boundaries and handling navigation between pages'], correctAnswer: 2, explanation: 'Suspense wraps lazy-loaded components and shows a fallback UI while they load.' },
  { id: 'lt-21', question: 'What is the purpose of useCallback?', options: ['To create callback functions that can dispatch state updates to a reducer', 'To provide error handling and automatic retry logic for async callbacks', 'To schedule a function to run synchronously after the component renders', 'To memoize a function reference so it only changes when dependencies change'], correctAnswer: 3, explanation: 'useCallback returns a memoized function that only changes when dependencies change.' },
  { id: 'lt-22', question: 'What is the difference between useMemo and useCallback?', options: ['They are functionally identical and can be used interchangeably in all cases', 'useMemo caches a computed value; useCallback caches a function reference', 'useMemo is intended for class components, useCallback for functional components', 'useMemo recalculates on every render while useCallback runs only on mount'], correctAnswer: 1, explanation: 'useMemo returns the memoized result of calling a function. useCallback returns the memoized function itself.' },
  { id: 'lt-23', question: 'What does <Outlet /> do in React Router?', options: ['Renders the matched child route\'s element in nested routes', 'Creates a navigation link that highlights when the route matches', 'Redirects the user from one route path to a different route path', 'Renders a 404 error page when no child routes match the current URL'], correctAnswer: 0, explanation: '<Outlet /> is a placeholder where the matched child route\'s element will be rendered.' },
  { id: 'lt-24', question: 'How do you access URL parameters like /users/:id?', options: ['window.location.params', 'props.match.params', 'useParams() hook', 'useLocation().params'], correctAnswer: 2, explanation: 'useParams() returns an object of key/value pairs from the current URL matching the dynamic segments.' },
  { id: 'lt-25', question: 'What is a Higher-Order Component (HOC)?', options: ['A component that is rendered at the top of the DOM above all other elements', 'A component that utilizes every available React hook in its implementation', 'A component that accepts and manages more than ten props simultaneously', 'A function that takes a component and returns a new enhanced component'], correctAnswer: 3, explanation: 'A HOC accepts a component and returns a new component with additional behavior or props.' },
  { id: 'lt-26', question: 'What is the compound component pattern?', options: ['Components sharing implicit state via context while consumers control composition', 'Using multiple useState hooks together to manage related pieces of state', 'Deeply nesting div elements to create complex layout structures in JSX', 'Applying TypeScript generics to enforce strict type safety on component props'], correctAnswer: 0, explanation: 'Compound components share internal state via context while letting the consumer control composition.' },
  { id: 'lt-27', question: 'What is the render props pattern?', options: ['Using the render() lifecycle method from class-based React components', 'A pattern specifically designed for server-side rendering of components', 'Serializing all component props as text strings before passing them down', 'A component receives a function prop and calls it to determine what to render'], correctAnswer: 3, explanation: 'Render props: a component receives a function and calls it with data, letting the consumer decide what to render.' },
  { id: 'lt-28', question: 'Why should component names start with a capital letter?', options: ['It is just a convention with no technical enforcement by the framework', 'React treats lowercase tags as HTML elements and uppercase as components', 'It improves runtime performance by enabling faster component tree lookups', 'It is a strict requirement enforced by the TypeScript compiler for JSX files'], correctAnswer: 1, explanation: 'React uses the case of the first letter to distinguish between HTML elements and custom components.' },
  { id: 'lt-29', question: 'What is StrictMode used for?', options: ['Enforcing TypeScript type-checking at runtime in addition to compile time', 'Optimizing the app for production by stripping out development-only code', 'Highlighting potential problems by running extra checks in development', 'Preventing unauthorized access by validating user sessions on each render'], correctAnswer: 2, explanation: 'StrictMode helps identify potential problems by double-invoking certain functions in development.' },
  { id: 'lt-30', question: 'What is React.Fragment (<> </>) for?', options: ['Grouping elements without adding extra DOM nodes', 'Creating a portal to render children in a different DOM tree', 'Caching components in memory to prevent unnecessary re-renders', 'Breaking a single component into smaller reusable sub-components'], correctAnswer: 0, explanation: 'Fragments let you group multiple elements without adding an extra wrapper DOM node.' },
  { id: 'lt-31', question: 'What causes an infinite loop in useEffect?', options: ['Declaring the effect callback as an async function returning a Promise', 'Returning a cleanup function that removes event listeners on unmount', 'Using multiple useEffect hooks that each watch different state variables', 'Setting state inside useEffect without proper dependencies, causing re-trigger'], correctAnswer: 3, explanation: 'If useEffect updates state that triggers re-render and the effect runs again due to wrong dependencies, it loops.' },
  { id: 'lt-32', question: 'How do you handle multiple form inputs with one onChange handler?', options: ['Use a separate ref for each input element and read values from the DOM directly', 'Declare separate state variables and individual handlers for every form field', 'Use a state object and computed property names: [e.target.name]: e.target.value', 'Each input requires its own dedicated handler function — there is no shared way'], correctAnswer: 2, explanation: 'Give each input a name attribute and use computed property names to update the correct field.' },
  { id: 'lt-33', question: 'What does the useNavigate hook do?', options: ['Registers a new route definition in the React Router configuration', 'Returns a function for programmatic navigation', 'Navigates to an external URL outside the single-page application', 'Generates a breadcrumb trail based on the current route hierarchy'], correctAnswer: 1, explanation: 'useNavigate returns a function that lets you navigate programmatically.' },
  { id: 'lt-34', question: 'What is an "index" route in React Router?', options: ['Generates the index.html file used as the entry point for the SPA', 'A permanent redirect from any unknown path to the application homepage', 'The default child route that renders at the parent URL', 'A route specifically designed for handling query parameters and filters'], correctAnswer: 2, explanation: 'An index route renders at the parent\'s URL when no other child route matches.' },
  { id: 'lt-35', question: 'Why use AbortController when fetching in useEffect?', options: ['To cancel in-flight requests on unmount, preventing stale state updates', 'To improve request speed by compressing the fetch payload automatically', 'It is strictly required by the Fetch API specification for all requests', 'To implement automatic retry logic when network requests fail or timeout'], correctAnswer: 0, explanation: 'AbortController cancels in-flight requests, preventing state updates on unmounted components.' },
  { id: 'lt-36', question: 'What is "lifting state up"?', options: ['Moving all component state into a Redux store for centralized management', 'Moving state declarations inside a useEffect hook for deferred initialization', 'Persisting state to localStorage so it survives across page refreshes', 'Moving state to the closest common parent of components that share it'], correctAnswer: 3, explanation: 'Lifting state up means moving shared state to the closest common ancestor.' },
  { id: 'lt-37', question: 'What is derived state?', options: ['State that comes from props and should always be stored in a separate variable', 'State shared between sibling components via a common parent\'s props', 'State fetched from the server that is cached locally in the component', 'State that can be computed from existing state — duplicating can cause sync bugs'], correctAnswer: 3, explanation: 'Derived state is computable from existing state. A separate copy can get out of sync, causing bugs.' },
  { id: 'lt-38', question: 'What does <NavLink> do that <Link> does not?', options: ['Navigates to external URLs outside the current single-page application', 'Supports active state styling based on the current URL', 'Opens navigation links in a new browser tab or window by default', 'Automatically preloads the route component when hovering over the link'], correctAnswer: 1, explanation: 'NavLink adds support for an "active" class/style when its path matches the current URL.' },
  { id: 'lt-39', question: 'What is the lazy initializer pattern for useState?', options: ['Wrapping the initial value in a setTimeout to defer its computation', 'Setting the initial state to null and populating it after the first render', 'Passing a function to useState that runs only once to compute the initial value', 'Using a useEffect on mount to compute and set the initial state value'], correctAnswer: 2, explanation: 'useState(() => expensive()) accepts a function that runs only on the first render.' },
  { id: 'lt-40', question: 'When should you use a key prop other than the array index?', options: ['When items can be reordered, inserted, or deleted', 'Never — using the array index is always the correct approach', 'Only when using TypeScript, since it enforces strict key types', 'Only in production builds where React enables strict consistency'], correctAnswer: 0, explanation: 'Index keys cause bugs when items are reordered, inserted, or deleted. Use stable, unique IDs.' },
  { id: 'lt-41', question: 'What is the purpose of an Error Boundary?', options: ['To validate form inputs and display field-level error messages to users', 'To prevent React runtime warnings from appearing in the browser console', 'To intercept and handle HTTP errors returned from network API requests', 'Catch JavaScript errors in a component tree and display a fallback UI'], correctAnswer: 3, explanation: 'Error Boundaries catch rendering errors in child components and show a fallback UI.' },
  { id: 'lt-42', question: 'Can you use hooks inside a conditional statement?', options: ['Yes, hooks can be called anywhere including inside conditions and loops', 'Only in class components — functional components have different rules', 'Only the useState hook can be conditional; other hooks cannot be used', 'No — hooks must be called at the top level, in the same order every render'], correctAnswer: 3, explanation: 'Hooks must be called at the top level to ensure consistent ordering between renders.' },
  { id: 'lt-43', question: 'What is the correct React render + effect lifecycle order?', options: ['Effect runs first → Component renders → Cleanup executes afterward', 'Render → Paint → Effect → (update: Cleanup → Render → Paint → Effect)', 'Cleanup runs first → Component renders → Effect executes afterward', 'Effect fires immediately → Cleanup runs → Component renders to the screen'], correctAnswer: 1, explanation: 'React renders, browser paints, then effects run. On updates, cleanup runs before the new effect.' },
  { id: 'lt-44', question: 'What does createContext do?', options: ['Creates a context object to share values across the component tree', 'Creates a new standalone component with its own isolated state scope', 'Wraps existing components with additional style and theme properties', 'Creates memoized event handlers that persist across component renders'], correctAnswer: 0, explanation: 'createContext creates a Context object for sharing values without prop drilling.' },
  { id: 'lt-45', question: 'What is "composition over inheritance" in React?', options: ['Extending components using class inheritance to share common behavior', 'Using TypeScript interfaces to enforce strict type hierarchies on components', 'Building UIs by combining components via props and children rather than inheritance', 'Avoiding the children prop entirely and passing all data through explicit props'], correctAnswer: 2, explanation: 'React favors composition — building UIs from small, reusable components — over class inheritance.' },
  { id: 'lt-46', question: 'What is a pure component?', options: ['A component that is written exclusively in TypeScript with full type annotations', 'Any functional component regardless of whether it has side effects or not', 'A component that does not accept or render any children inside its body', 'Always renders the same output for the same props, with no side effects during render'], correctAnswer: 3, explanation: 'A pure component returns the same JSX for the same props/state, with no render-phase side effects.' },
  { id: 'lt-47', question: 'How can you debounce a search input in React?', options: ['Call the search API on every single keystroke without any delay', 'Use useEffect with setTimeout and clear it in cleanup', 'Wrap the input component in React.memo to limit re-renders', 'Disable the input field between search API calls and re-enable after'], correctAnswer: 1, explanation: 'Use setTimeout in useEffect and clearTimeout in cleanup — changes before timeout fire cancel the previous call.' },
  { id: 'lt-48', question: 'What is the difference between controlled and uncontrolled components?', options: ['Controlled: value driven by state. Uncontrolled: value in the DOM, read via ref', 'Controlled components use class syntax while uncontrolled use function syntax', 'They are functionally identical and can be used interchangeably in all cases', 'Uncontrolled components cannot have an onChange handler attached to them at all'], correctAnswer: 0, explanation: 'Controlled: React state drives form value. Uncontrolled: DOM holds the value, accessed via ref.' },
  { id: 'lt-49', question: 'What does the spread operator do with props: <C {...obj} />?', options: ['Creates a deep copy of the component with all of its internal state', 'Merges the component definition and the object into a new component', 'Passes all properties of obj as individual props to C', 'Converts the object to a JSON string and passes it as a single prop'], correctAnswer: 2, explanation: 'The spread operator passes each property of the object as a separate prop to the component.' },
  { id: 'lt-50', question: 'Why can an object literal in useEffect deps cause infinite re-runs?', options: ['Objects are not a valid type for the useEffect dependency array values', 'A new reference is created each render, so React thinks the dep changed', 'The useEffect hook does not support objects as dependency array entries', 'It triggers a syntax error because objects cannot be compared by React'], correctAnswer: 1, explanation: 'React uses Object.is() for deps comparison. A new object literal has a new reference each render.' },
  { id: 'lt-51', question: 'Where is the best place to apply code splitting?', options: ['Every single component should be individually code-split for best performance', 'Inside useEffect hooks to defer loading of expensive side-effect logic', 'Only the root application component should be wrapped in a lazy boundary', 'Route boundaries — loading each page on demand'], correctAnswer: 3, explanation: 'Route-based splitting ensures each page\'s code is loaded only when the user navigates to it.' },
  { id: 'lt-52', question: 'How do custom hooks share logic between components?', options: ['By encapsulating reusable logic — each caller gets its own copy of state', 'By sharing the same state instance across all components that call the hook', 'By using global variables that are accessible from any component in the app', 'By rendering shared UI elements that are injected into the calling component'], correctAnswer: 0, explanation: 'Each component calling a custom hook gets its own independent copy of the hook\'s state.' },
  { id: 'lt-53', question: 'What must a custom hook\'s name start with?', options: ['get', 'handle', 'use', 'on'], correctAnswer: 2, explanation: 'Custom hooks must start with "use" so React can enforce the rules of hooks.' },
  { id: 'lt-54', question: 'What is reconciliation in React?', options: ['The process of merging two separate state objects into a single state value', 'The process of diffing virtual DOM trees and updating the real DOM efficiently', 'The mechanism for connecting React components to an external API data source', 'The lifecycle phase responsible for cleaning up effects before a component unmounts'], correctAnswer: 1, explanation: 'Reconciliation diffs two virtual DOM trees to determine the minimum number of real DOM changes.' },
  { id: 'lt-55', question: 'When does a component re-render?', options: ['Only when its own internal state changes via a setState or dispatch call', 'Only when the props passed to it from the parent component have changed', 'When its state changes, parent re-renders, or a subscribed context changes', 'Only when the user directly interacts with the component via mouse or keyboard'], correctAnswer: 2, explanation: 'A component re-renders when its state changes, parent re-renders, or a consumed context changes.' },
  { id: 'lt-56', question: 'What is a portal in React?', options: ['A mechanism for redirecting users between different pages in the application', 'A specialized context provider that broadcasts theme values to all children', 'A security feature that sandboxes component rendering in an isolated scope', 'A way to render children into a DOM node outside the parent\'s hierarchy'], correctAnswer: 3, explanation: 'createPortal renders a child into a different DOM node, useful for modals and tooltips.' },
  { id: 'lt-57', question: 'What happens if you forget cleanup when adding an event listener in useEffect?', options: ['Nothing — React automatically manages and removes event listeners for you', 'The listener accumulates on each re-render, causing memory leaks', 'The application throws an error and crashes on the next user interaction', 'The listener is automatically garbage collected when the component unmounts'], correctAnswer: 1, explanation: 'Without cleanup, each render adds another listener without removing the old one — memory leak and duplicate calls.' },
  { id: 'lt-58', question: 'What type of value can useState initial state be?', options: ['Only primitive values like strings, numbers, and booleans are accepted', 'Only string values are supported as the initial state for useState hooks', 'Only objects and arrays can be used because state must be serializable', 'Any value: primitives, objects, arrays, null, or a lazy initializer function'], correctAnswer: 3, explanation: 'useState accepts any value as initial state, including a function for lazy initialization.' },
  { id: 'lt-59', question: 'What does the path="*" route do in React Router v6?', options: ['It matches all defined routes simultaneously and renders them in parallel', 'It enables advanced regex-based pattern matching for route path definitions', 'It creates a wildcard redirect that forwards all traffic to the root route', 'Acts as a catch-all for unmatched URLs (404 page)'], correctAnswer: 3, explanation: 'path="*" matches any URL that doesn\'t match other defined routes, commonly used for 404 pages.' },
  { id: 'lt-60', question: 'What is batching in React?', options: ['Grouping multiple components together into a single compound component unit', 'React groups multiple state updates into a single re-render for performance', 'Splitting application code into smaller chunks that load independently', 'Running multiple useEffect cleanup functions in parallel during unmounting'], correctAnswer: 1, explanation: 'React batches state updates from the same event handler into a single re-render.' },
];

export function getRandomLevelTest(count = 30): QuizQuestion[] {
  const shuffled = [...levelTestPool];
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }
  return shuffled.slice(0, count).map((q, i) => ({ ...q, id: `lt-r-${i}` }));
}
